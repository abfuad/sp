<form method="post" action="{{ path('app_payment_month_delete', {'id': payment_month.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ payment_month.id) }}">
    <button class="btn">Delete</button>
</form>
