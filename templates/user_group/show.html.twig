{% extends 'base.html.twig' %}

{% block title %}UserGroup{% endblock %}

{% block body %}
    <h1>UserGroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ user_group.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ user_group.code }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ user_group.description }}</td>
            </tr>
            <tr>
                <th>CreatedBy</th>
                <td>{{ user_group.createdBy }}</td>
            </tr>
            <tr>
                <th>UpdatedBy</th>
                <td>{{ user_group.updatedBy }}</td>
            </tr>
            <tr>
                <th>DeletedBy</th>
                <td>{{ user_group.deletedBy }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ user_group.createdAt ? user_group.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ user_group.updatedAt ? user_group.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_group_index') }}">back to list</a>

    <a href="{{ path('app_user_group_edit', {'id': user_group.id}) }}">edit</a>

    {{ include('user_group/_delete_form.html.twig') }}
{% endblock %}
