<!-- Navbar -->
<nav class="main-header navbar navbar-expand navbar-lightblue navbar-light bg-primary">
	{# class="main-header navbar navbar-expand navbar-white navbar-light"> #}
	<!-- Left navbar links -->
		<ul class="navbar-nav"> <li class="nav-item">
			<a class="nav-link" data-widget="pushmenu" href="#">
				<i class="fas fa-bars"></i>
			</a>
		</li>
		<li class="nav-item d-none d-sm-inline-block">
			<a href="{{ path('app_home') }}" class="nav-link text-white">Home</a>
		</li>
		{# <li class="nav-item d-none d-sm-inline-block">
																	<a href="#" class="nav-link">Contact</a>
																</li> #}
	</ul>

	
		<ul class="navbar-nav ml-auto"> <!-- Messages Dropdown Menu -->


			<li class="nav-item dropdown"> <a class="nav-link" data-toggle="dropdown" href="#">
				<i class="fas fa-language"></i>
			</a>
			<div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">

				<form class="text-light" id="language"  method="post" {% if app.user %} action="{{ path('change_locale')}}" {% else %} action="{{ path('change_locale')}}" {% endif %}>
					<div class="input-group input-group-sm">
						<select name="lang" id="lang" class="form-control ">

							<option value="en" {% if app.request.getLocale() == 'en' %} selected {% endif %}>English</option>
							<option value="am" {% if app.request.getLocale() == 'am' %} selected {% endif %}>አማርኛ</option>
							<option value="or" {% if app.request.getLocale() == 'or' %} selected {% endif %}>Afaan Oromo</option>
						</select>

					</div>
				</form>
			</div>

		</li>
		<li class="nav-item dropdown">
			<a class="nav-link" data-toggle="dropdown" href="#" aria-expanded="true">
				<i class="fas fa-th-large"></i>
			</a>
			<div class="dropdown-menu dropdown-menu-lg dropdown-menu-right ">
				<span class="dropdown-item dropdown-header">Setting</span>
				<div class="dropdown-divider"></div>
				{# <a href="{{ path('user_profile') }}" class="dropdown-item">
					<i class="fas fa-user mr-2"></i>Profile
				</a> #}
					
				<div class="dropdown-divi#der"></div>
			</div>

		</li>
		{% if app.user %}
		
		<li  class="nav-item">
			<a href="{{ path('app_logout')}}" class="nav-link">
				<i class="fa fa-sign-out-alt" aria-hidden="true"></i>{% trans %}Logout{% endtrans %}
			</a>
		</li>
		{% else %}
			<li  class="nav-item">
			<a href="{{ path('app_login')}}" class="nav-link">
				<i class="fa fa-sign-in-alt" aria-hidden="true"></i>{% trans %}Login{% endtrans %}
			</a>
		</li>
		{% endif %}


	</ul>
</nav>
