{% extends 'base.html.twig' %}

{% block title %}SpecialIncome{% endblock %}

{% block body %}
    <h1>SpecialIncome</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ special_income.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ special_income.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ special_income.code }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ special_income.description }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ special_income.amount }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ special_income.createdAt ? special_income.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ special_income.updatedAt ? special_income.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_special_income_index') }}">back to list</a>

    <a href="{{ path('app_special_income_edit', {'id': special_income.id}) }}">edit</a>

    {{ include('special_income/_delete_form.html.twig') }}
{% endblock %}
