{% extends 'base.html.twig' %}

{% block title %}Budget{% endblock %}

{% block body %}
    <h1>Budget</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ budget.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ budget.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ budget.code }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ budget.description }}</td>
            </tr>
            <tr>
                <th>PlanValue</th>
                <td>{{ budget.planValue }}</td>
            </tr>
            <tr>
                <th>ActualValue</th>
                <td>{{ budget.actualValue }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ budget.createdAt ? budget.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ budget.updatedAt ? budget.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_budget_index') }}">back to list</a>

    <a href="{{ path('app_budget_edit', {'id': budget.id}) }}">edit</a>

    {{ include('budget/_delete_form.html.twig') }}
{% endblock %}
