{% extends 'base.html.twig' %}

{% block title %}IncomeType{% endblock %}

{% block body %}
    <h1>IncomeType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ income_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ income_type.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ income_type.code }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ income_type.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ income_type.createdAt ? income_type.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ income_type.updatedAt ? income_type.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_income_type_index') }}">back to list</a>

    <a href="{{ path('app_income_type_edit', {'id': income_type.id}) }}">edit</a>

    {{ include('income_type/_delete_form.html.twig') }}
{% endblock %}
