<form method="post" action="{{ path('app_budget_expense_plan_delete', {'id': budget_expense_plan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ budget_expense_plan.id) }}">
    <button class="btn">Delete</button>
</form>
