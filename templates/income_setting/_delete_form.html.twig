<form method="post" action="{{ path('app_income_setting_delete', {'id': income_setting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ income_setting.id) }}">
    <button class="btn">Delete</button>
</form>
