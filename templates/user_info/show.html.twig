{% extends 'base.html.twig' %}

{% block title %}UserInfo{% endblock %}

{% block body %}
    <h1>UserInfo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_info.id }}</td>
            </tr>
            <tr>
                <th>CreatedBy</th>
                <td>{{ user_info.createdBy }}</td>
            </tr>
            <tr>
                <th>UpdatedBy</th>
                <td>{{ user_info.updatedBy }}</td>
            </tr>
            <tr>
                <th>DeletedBy</th>
                <td>{{ user_info.deletedBy }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ user_info.firstName }}</td>
            </tr>
            <tr>
                <th>MiddleName</th>
                <td>{{ user_info.middleName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ user_info.lastName }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ user_info.phone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user_info.email }}</td>
            </tr>
            <tr>
                <th>Kebele</th>
                <td>{{ user_info.kebele }}</td>
            </tr>
            <tr>
                <th>Sex</th>
                <td>{{ user_info.sex }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ user_info.age }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ user_info.createdAt ? user_info.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ user_info.updatedAt ? user_info.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_info_index') }}">back to list</a>

    <a href="{{ path('app_user_info_edit', {'id': user_info.id}) }}">edit</a>

    {{ include('user_info/_delete_form.html.twig') }}
{% endblock %}
